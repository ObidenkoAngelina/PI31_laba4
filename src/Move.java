class Move {
    int row;
    int col;
    boolean isCorrect;

    public Move(int row, int col, boolean isCorrect) {
        this.row = row;
        this.col = col;
        this.isCorrect = isCorrect;
    }
}
